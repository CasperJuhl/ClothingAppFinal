<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/logoColor</item><!--Changed to match "red" theme in logo-->
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Styles of RecyclerView-->
    <style name="ClothStyle">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/allBlack</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@color/backgroundColor</item>
    </style>
    <style name="ClothStyleIfImage">
        <item name="android:background">@color/backgroundColor</item>
    </style>

    <!--Styles of ScrollView-->
    <style name="ClothStyleScroll">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/allBlack</item>
        <item name="android:textSize">25sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@color/backgroundColor</item>
    </style>
</resources>
